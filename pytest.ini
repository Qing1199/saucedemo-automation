[pytest]
testpaths = tests
addopts = -v --tb=short --html=reports/report.html --self-contained-html

markers =
    smoke: critical path tests — run before every deployment
    regression: full regression suite — run nightly
    sanity: quick sanity check after a hotfix

log_cli = true
log_cli_level = INFO